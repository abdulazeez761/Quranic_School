@{
    ViewData["Title"] = "أكاديمية ذِكْر - خطط الاشتراك";
}

@section Styles {
    <link rel="stylesheet" href="~/css/plan.css" />
}

@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

<div class="full-width-container">
    <!-- Header -->
    <div class="header">
        <h1>@Localizer["HeaderTitle"]</h1>
        <p class="subtitle">@Localizer["HeaderSubtitle"]</p>
    </div>

    <!-- Subscription Plans -->
    <div class="plans-section">
        <div class="plans-grid">
            <!-- Quran Memorization Plan -->
            <div class="plan-card">
                <div class="plan-header">
                    <h3 class="plan-title">@Localizer["QuranPlanTitle"]</h3>
                    <p class="plan-age">@Localizer["QuranPlanAge"]</p>
                    <div class="plan-price">$49</div>
                    <div class="plan-duration">@Localizer["Monthly"]</div>
                </div>
                <p class="plan-description">@Localizer["QuranPlanDesc"]</p>
                <ul class="plan-features">
                    <li>@Localizer["QuranF1"]</li>
                    <li>@Localizer["QuranF2"]</li>
                    <li>@Localizer["QuranF3"]</li>
                    <li>@Localizer["QuranF4"]</li>
                    <li>@Localizer["QuranF5"]</li>
                </ul>
                <button class="plan-button">@Localizer["ChoosePlan"]</button>
            </div>

            <!-- Faith Education Plan -->
            <div class="plan-card featured">
                <div class="plan-header">
                    <h3 class="plan-title">@Localizer["FaithPlanTitle"]</h3>
                    <p class="plan-age">@Localizer["FaithPlanAge"]</p>
                    <div class="plan-price">$69</div>
                    <div class="plan-duration">@Localizer["Monthly"]</div>
                </div>
                <p class="plan-description">@Localizer["FaithPlanDesc"]</p>
                <ul class="plan-features">
                    <li>@Localizer["FaithF1"]</li>
                    <li>@Localizer["FaithF2"]</li>
                    <li>@Localizer["FaithF3"]</li>
                    <li>@Localizer["FaithF4"]</li>
                    <li>@Localizer["FaithF5"]</li>
                    <li>@Localizer["FaithF6"]</li>
                </ul>
                <button class="plan-button">@Localizer["ChoosePlan"]</button>
            </div>

            <!-- Reinforcement and Follow-up Plan -->
            <div class="plan-card">
                <div class="plan-header">
                    <h3 class="plan-title">@Localizer["FollowupPlanTitle"]</h3>
                    <p class="plan-age">@Localizer["FollowupPlanAge"]</p>
                    <div class="plan-price">$39</div>
                    <div class="plan-duration">@Localizer["Monthly"]</div>
                </div>
                <p class="plan-description">@Localizer["FollowupPlanDesc"]</p>
                <ul class="plan-features">
                    <li>@Localizer["FollowupF1"]</li>
                    <li>@Localizer["FollowupF2"]</li>
                    <li>@Localizer["FollowupF3"]</li>
                    <li>@Localizer["FollowupF4"]</li>
                    <li>@Localizer["FollowupF5"]</li>
                </ul>
                <button class="plan-button">@Localizer["ChoosePlan"]</button>
            </div>
        </div>
    </div>

    <!-- Features Section -->
    <div class="features-section">
        <h2 class="features-title">@Localizer["WhyChooseTitle"]</h2>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">🏠</div>
                <h3 class="feature-title">@Localizer["Feature1Title"]</h3>
                <p class="feature-description">@Localizer["Feature1Desc"]</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🎮</div>
                <h3 class="feature-title">@Localizer["Feature2Title"]</h3>
                <p class="feature-description">@Localizer["Feature2Desc"]</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">👨‍👩‍👧‍👦</div>
                <h3 class="feature-title">@Localizer["Feature3Title"]</h3>
                <p class="feature-description">@Localizer["Feature3Desc"]</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🏆</div>
                <h3 class="feature-title">@Localizer["Feature4Title"]</h3>
                <p class="feature-description">@Localizer["Feature4Desc"]</p>
            </div>
        </div>
    </div>

    <!-- Free Trial Section -->
    <div class="free-trial-section">
        <h2 class="free-trial-title">@Localizer["FreeTrialTitle"]</h2>
        <p style="margin-bottom: 40px; font-size: 1.1rem;">@Localizer["FreeTrialDesc"]</p>
        <form class="free-trial-form">
            <div class="form-group">
                <label for="fullName">@Localizer["FullName"]</label>
                <input type="text" id="fullName" name="fullName" required>
            </div>
            <div class="form-group">
                <label for="childAge">@Localizer["ChildAge"]</label>
                <select id="childAge" name="childAge" required>
                    <option value="">@Localizer["SelectAge"]</option>
                    <option value="5-7">@Localizer["Age5_7"]</option>
                    <option value="8-10">@Localizer["Age8_10"]</option>
                    <option value="11-13">@Localizer["Age11_13"]</option>
                    <option value="14-17">@Localizer["Age14_17"]</option>
                </select>
            </div>
            <div class="form-group">
                <label for="country">@Localizer["Country"]</label>
                <input type="text" id="country" name="country" required>
            </div>
            <div class="form-group">
                <label for="whatsapp">@Localizer["WhatsAppNumber"]</label>
                <input type="tel" id="whatsapp" name="whatsapp" required>
            </div>
            <div class="form-group">
                <label for="notes">@Localizer["Notes"]</label>
                <textarea id="notes" name="notes" placeholder="@Localizer["NotesPlaceholder"]"></textarea>
            </div>
            <button type="submit" class="submit-btn">@Localizer["SubmitRequest"]</button>
        </form>
    </div>

    <!-- Contact Section -->
    <div class="contact-section">
        <h2 class="contact-title">@Localizer["QuestionsTitle"]</h2>
        <p style="margin-bottom: 40px; font-size: 1.1rem;">@Localizer["QuestionsSubtitle"]</p>
        <div class="contact-info">
            <div class="contact-item">
                <div style="font-size: 1.2rem; margin-bottom: 10px;">@Localizer["WhatsAppLabel"]</div>
                <a href="https://wa.me/1234567890">+1 (234) 567-890</a>
            </div>
            <div class="contact-item">
                <div style="font-size: 1.2rem; margin-bottom: 10px;">@Localizer["WebsiteLabel"]</div>
                <a href="https://dhikr-academy.com">dhikr-academy.com</a>
            </div>
            <div class="contact-item">
                <div style="font-size: 1.2rem; margin-bottom: 10px;">@Localizer["EmailLabel"]</div>
                <a href="mailto:info@dhikr-academy.com">info@dhikr-academy.com</a>
            </div>
        </div>
        <div class="closing-message">
            <p>@Localizer["ClosingMessage"]</p>
        </div>
    </div>
</div>

<script>
    // Form submission handler
    document.querySelector('.free-trial-form').addEventListener('submit', function (e) {
        e.preventDefault();

        // Collect form data
        const formData = new FormData(this);
        const data = Object.fromEntries(formData);

        // Show success message
        alert('تم إرسال طلبك بنجاح! سنتواصل معك خلال 24 ساعة إن شاء الله.');

        // Reset form
        this.reset();
    });

    // Plan selection handlers
    document.querySelectorAll('.plan-button').forEach(button => {
        button.addEventListener('click', function () {
            const planTitle = this.closest('.plan-card').querySelector('.plan-title').textContent;
            alert(`تم اختيار خطة: ${planTitle}\nسيتم تحويلك إلى صفحة التسجيل...`);
        });
    });

    // Add smooth scrolling to all links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth'
                });
            }
        });
    });

    // Add animation on scroll
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver(function (entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, observerOptions);

    // Observe all cards and sections
    document.querySelectorAll('.plan-card, .feature-card, .contact-section').forEach(el => {
        el.style.opacity = '0';
        el.style.transform = 'translateY(20px)';
        el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
        observer.observe(el);
    });
</script>